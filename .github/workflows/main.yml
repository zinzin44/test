name: Windows-RDP-Ghost-Edition-Fixed
on:
  workflow_dispatch:
    inputs:
      auth_key_b64:
        description: 'أدخل مفتاح التشغيل (المشفر 4 مرات)'
        required: true
      rdp_password_b64:
        description: 'أدخل كلمة سر الـ RDP (المشفرة 4 مرات)'
        required: true

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: فك التشفير الرباعي والتحقق
        shell: pwsh
        run: |
          function Decode-Quad ($inputString) {
              $current = $inputString.Trim()
              for ($i = 0; $i -lt 4; $i++) {
                  $current = [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($current)).Trim()
              }
              return $current
          }
          try {
              $decoded_key = Decode-Quad "${{ github.event.inputs.auth_key_b64 }}"
              $decoded_pass = Decode-Quad "${{ github.event.inputs.rdp_password_b64 }}"
              if ($decoded_key -eq "${{ secrets.MY_START_KEY }}") {
                  Write-Host "✅ تم التحقق بنجاح" -ForegroundColor Green
                  echo "RDP_PASS=$decoded_pass" >> $env:GITHUB_ENV
              } else {
                  Write-Host "❌ خطأ في المطابقة" -ForegroundColor Red
                  exit 1
              }
          } catch {
              Write-Host "❌ فشل فك التشفير" -ForegroundColor Red
              exit 1
          }

      - name: تحميل وتنكر الأداة
        shell: pwsh
        run: |
          curl -L --output sys_driver.exe https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe

      - name: إعداد المستخدم والأمان
        shell: pwsh
        run: |
          $name = "GhostAdmin"
          $pass = $env:RDP_PASS
          $secPassword = ConvertTo-SecureString $pass -AsPlainText -Force
          New-LocalUser -Name $name -Password $secPassword -FullName "System Managed Account"
          Add-LocalGroupMember -Group "Administrators" -Member $name
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0

      - name: تشغيل النفق المجاني (تصحيح الأوامر)
        shell: pwsh
        run: |
          # تم حذف --http-header لأنه لا يدعم بروتوكول rdp:// في الأنفاق السريعة
          Start-Process ./sys_driver.exe -ArgumentList "tunnel --url rdp://localhost:3389" -RedirectStandardError "logs.txt"
          
          Write-Host "جاري توليد الرابط المجاني..." -ForegroundColor Cyan
          Start-Sleep -Seconds 35
          
          $logContent = Get-Content "logs.txt"
          $raw_url = $logContent | Select-String "https://.*trycloudflare.com" | ForEach-Object { $_.Matches.Value }
          
          if ($raw_url) {
              $b64_url = [System.Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($raw_url))
              Write-Host "----------------------------------------------"
              Write-Host "رابط السيرفر المشفر (فك التشفير لاستخدامه):" -ForegroundColor Green
              Write-Host $b64_url -ForegroundColor Yellow
              Write-Host "----------------------------------------------"
          } else {
              Write-Host "❌ فشل استخراج الرابط. محتوى السجل:" -ForegroundColor Red
              $logContent
          }

      - name: المراقبة ومفتاح الإغلاق
        shell: pwsh
        run: |
          Write-Host "السيرفر يعمل... المراقبة نشطة."
          for ($i = 0; $i -lt 360; $i++) {
              if (Test-Path "stop.txt") {
                  if ((Get-Content "stop.txt").Trim() -eq "STOP") {
                      Write-Host "🛑 تم الإغلاق اليدوي." -ForegroundColor Red
                      exit 0
                  }
              }
              Start-Sleep -Seconds 60
          }
