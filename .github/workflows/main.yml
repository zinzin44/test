name: Windows-RDP-Ghost-Extreme-v4
on:
  workflow_dispatch:
    inputs:
      auth_key_b64:
        description: 'أدخل مفتاح التشغيل (مشفر 4 مرات)'
        required: true
      rdp_password_b64:
        description: 'أدخل كلمة سر الـ RDP (مشفرة 4 مرات)'
        required: true

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: فك التشفير والتحقق
        shell: pwsh
        run: |
          function Decode-Quad ($inputString) {
              $current = $inputString.Trim()
              for ($i = 0; $i -lt 4; $i++) {
                  $current = [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($current)).Trim()
              }
              return $current
          }
          $decoded_key = Decode-Quad "${{ github.event.inputs.auth_key_b64 }}"
          $decoded_pass = Decode-Quad "${{ github.event.inputs.rdp_password_b64 }}"
          if ($decoded_key -eq "${{ secrets.MY_START_KEY }}") {
              echo "RDP_PASS=$decoded_pass" >> $env:GITHUB_ENV
          } else { exit 1 }

      - name: تحميل Cloudflared وإعداد RDP
        run: |
          curl -L --output cloudflared.exe https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0

      - name: إنشاء المستخدم
        run: |
          $name = "GhostAdmin"
          $pass = $env:RDP_PASS
          net user $name $pass /add
          net localgroup administrators $name /add

      - name: استخراج وتشفير الرابط (طريقة مضمونة 100%)
        shell: pwsh
        run: |
          # تشغيل النفق كعملية خلفية (مثل كودك الناجح)
          Start-Process ./cloudflared.exe -ArgumentList "tunnel --url rdp://localhost:3389"
          
          Write-Host "جاري توليد الرابط السري... انتظر 30 ثانية" -ForegroundColor Cyan
          Start-Sleep -Seconds 30
          
          # استخراج الرابط مباشرة من مخرجات البرنامج كما فعلت أنت
          # ولكن بدلاً من طباعته، سنقوم بتشفيره فوراً
          $raw_url = ./cloudflared.exe tunnel --url rdp://localhost:3389 2>&1 | Select-String "https://[a-z0-9-]+\.trycloudflare\.com" | ForEach-Object { $_.Matches.Value }
          
          if ($raw_url) {
              $current_url = $raw_url[0].Trim() # التقاط أول رابط يظهر
              # التشفير الرباعي
              for ($i = 0; $i -lt 4; $i++) {
                  $current_url = [System.Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($current_url))
              }
              Write-Host "----------------------------------------------"
              Write-Host "✅ تم التشفير بنجاح. فك التشفير 4 مرات للحصول على الرابط:"
              Write-Host $current_url -ForegroundColor Yellow
              Write-Host "----------------------------------------------"
          } else {
              Write-Host "❌ لم يتم العثور على رابط. أعد المحاولة."
              exit 1
          }

      - name: إبقاء السيرفر نشطاً
        run: |
          $n = 360
          while ($n -gt 0) {
            Write-Host "سيرفر الشبح يعمل... المتبقي: $n دقيقة"
            Start-Sleep -Seconds 60
            $n--
          }
